server.port=2020
spring.profiles.active=@profile.active@
spring.application.name=sso-member

# Springboot2.6以后将SpringMVC 默认路径匹配策略从AntPathMatcher 更改为PathPatternParser，使用swagger2时不加该配置会报错
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

logging.config=classpath:log4j2-spring.xml
logging.file.path=E:/vmShare/centos64/${spring.application.name}